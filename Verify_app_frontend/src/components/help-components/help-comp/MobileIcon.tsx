import { Smartphone } from 'lucide-react';
import styles from '../help-styled-comp/helpIcons.module.css';

const MobileIcon = () => {
  return <Smartphone className={styles.inlineIcon} />;
};

export default MobileIcon;
