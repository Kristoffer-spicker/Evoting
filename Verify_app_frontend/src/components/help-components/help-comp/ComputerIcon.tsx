import { Laptop } from 'lucide-react';
import styles from '../help-styled-comp/helpIcons.module.css';

const ComputerIcon = () => {
  return <Laptop className={styles.inlineIcon} />;
};

export default ComputerIcon;
